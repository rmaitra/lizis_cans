<% provide(:title, 'Home') %>
    <h1>Blog Posting!</h1>
    <p>
        Welcome to your new application!
    </p>
    <div>
        <% @microposts.each do |micropost| %>
          <h3> <%= @users[micropost.user_id-1].name %></h3>
          <p><%= micropost.content %></p>
        <% end %>
    </div>
